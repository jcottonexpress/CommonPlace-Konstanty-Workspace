# Candidate development values only. Do not use production credentials.
DATABASE_URL=postgresql://candidate-local-database
SESSION_SECRET=replace-in-replit-secrets
PUBLIC_APP_URL=http://localhost
NODE_ENV=development
